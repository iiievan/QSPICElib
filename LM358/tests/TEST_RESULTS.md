# LM358 family 0.1.0 — статус проверки

Дата подготовки: 2026-09-19.

**onsemi LM358: QSPICE/QPOST ВЫПОЛНЕНО 2026-09-19.**

Пользовательский прогон 11 исходных стендов дал 90 PASS, один намеренный WARN
вне диапазона 0…+70 °C и один ложный parser FAIL. `LM358_small_signal_test.out`
содержал четыре корректных результата в формате `value (at Time=...)`.
После исправления parser все они разбираются и проходят ожидания: итог исходного
набора — **94 PASS, 1 WARN, 0 FAIL**.

**TI LM358B/LM358BA: ожидает первого прогона.** Добавленная номинальная модель
и `LM358B_alias_temp_test` ещё не запускались в QSPICE в доступной среде.

## Что выполнено

| Проверка | Результат | Что это доказывает |
|---|---|---|
| Чтение таблицы и рисунков исходного PDF | Выполнено | Источник, тип компонента, номинальные значения и температурный рейтинг установлены |
| Bash syntax (`bash -n`) | Без ошибок | Синтаксис двух shell-скриптов |
| `bash tools/test_runner.sh` | 16/16 | Включая QPOST TRAN MIN/MAX с аннотацией `(at Time=...)` |
| onsemi LM358 | 11 стендов, 94 PASS, 1 ожидаемый WARN, 0 FAIL | Фактический QSPICE/QPOST-прогон и повторный разбор сохранённых `.out` |
| TI LM358B/LM358BA | Добавлены модель и отдельный стенд | Требуется фактический прогон QSPICE/QPOST |
| Архивные FDS4559 `.out` | Парсинг 5 файлов совпал с прежним | Совместимость чтения старых результатов; НЕ повторная симуляция FDS4559 |
| Численный разбор выбранных уравнений | Выполнено | Правдоподобность DC, малосигнальной передаточной функции и двух transient-оснасток; НЕ QSPICE-валидация |

## Дополнительная инженерная проверка уравнений

Воспроизводится отдельно, при наличии Python 3, NumPy и SciPy:

```bash
python tools/review_lm358_equations.py
```

Этот необязательный инструмент не нужен для штатных тестов библиотеки.
Он читает B-выражения из `LM358.txt`, решает ограниченный набор уравнений
и интегрирует их методом Radau. Он **не** загружает `.cir` в SPICE и не
проверяет поведение QSPICE, QPOST, `.step`, GUI-символов или pin mapping
полного netlist. Числа ниже нельзя считать фактическими `.meas`.

| Величина | Расчёт уравнений |
|---|---:|
| Входная ошибка, VS=5 В, VIC=0, VO≈1,4 В, 25 °C | 1,98666 мВ |
| AOL, RL=2 кОм, VS=15 В | 99994,7 В/В |
| CMRR, VS=5 В, VIC=0…3 В | 69,9986 дБ |
| PSRR, VS=5…30 В, VIC=0 | 100,009 дБ |
| Source / sink вдали от земли | 40 / 20 мА |
| Sink при VO=200 мВ | 50 мкА |
| VOH: 5 В / 2 кОм | 3,500 В |
| VOH: 30 В / 2 кОм | 27,4537 В |
| VOH: 30 В / 10 кОм | 28,000 В |
| VOL: 5 В / 10 кОм | 5,000 мВ |
| Gain: 1 / 100 / 1k / 10k / 100k / 1M Гц | 99,98 / 82,63 / 62,71 / 42,71 / 22,61 / −2,56 дБ |
| Время прохождения 1 В, rise / fall | 2,280 / 1,468 мкс |
| Малый импульс: минимум / максимум | 250,14 / 409,34 мВ |

Динамический fit приближённый: он не повторяет все точки рис.5/7 точно.
Подгонка двух полюсов уменьшила ошибку выброса малого импульса; уровни AC
в средней полосе примерно на 2,7 дБ выше графической прямой. Это записано
как погрешность модели, а не скрыто под статусом «точная модель».

## Что требуется для приёмки

1. Запустить `KEEP_RAW=1 ./run_tests.sh LM358` в QSPICE.
2. Проверить все 11 стендов, особенно DC-servo, AC-парсинг, отрицательные
   температуры и порядок выводов `LM358_DUAL`.
3. Сверить `.out` с таблицей происхождения критериев. Все hard FAIL устранить;
   каждый дополнительный WARN объяснить. Холодный рейтинг намеренно даёт WARN.
4. При изменениях параметров повторить весь компонентный набор; допуски не
   расширять ради PASS. Нужны реальные результаты для калибровки под QSPICE.
5. После этого записать фактический протокол, указав версию симулятора.

Температурная валидация реального LM358 при −35 °C не может быть закрыта
прогоном этой модели: это за пределами рабочего рейтинга указанного компонента.
