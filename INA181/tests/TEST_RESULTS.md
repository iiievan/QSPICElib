# INA181A1 0.2.0 validated - статус

Фактический прогон QSPICE/QPOST: 11 стендов, 60 PASS, 0 WARN, 0 FAIL.

Первоначальный единственный FAIL был ошибкой разбора форматов QPOST для DC
`MAX ... (at sweep=value)` и `FIND ... AT=`. После исправления parser извлёк
из сохранённого `.out` все четыре измерения линейности; все четыре прошли.
